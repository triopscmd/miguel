# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependency directories
node_modules/
jspm_packages/

# Build artifacts
dist/
build/
.next/ # If using Next.js (common in React ecosystem)
out/
public/build/ # For some setups

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Editor/IDE specific files
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# Prisma
# The generated Prisma Client is typically within node_modules,
# so node_modules/ already covers it.
# Add any specific database files if used locally (e.g., SQLite)
*.sqlite
*.sqlite-journal
*.db

# Test coverage
coverage/

# Vitest cache and temporary files
.vitest-cache/
.vitest/

# Vercel deployment directory
.vercel/

# TypeScript build output
# Typically covered by `dist/` or `build/`, but explicitly for `ts-node` or similar
*.tsbuildinfo